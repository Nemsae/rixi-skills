import styled from 'styled-components';

const Score = styled.p`
  position: absolute;
  left: 0;
  top: 10px;

  width: 150px;

  margin: 0;

  line-height: 150px;
  font-family: inherit;
  font-size: 48px;
  font-weight: 600;
`;

export default Score;
